package com.jerry.moneyapp.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.jerry.moneyapp.bean.Logg;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOGG".
*/
public class LoggDao extends AbstractDao<Logg, Void> {

    public static final String TABLENAME = "LOGG";

    /**
     * Properties of entity Logg.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property DeviceId = new Property(0, String.class, "deviceId", false, "DEVICE_ID");
        public final static Property CreateTime = new Property(1, String.class, "createTime", false, "CREATE_TIME");
        public final static Property Data = new Property(2, String.class, "data", false, "DATA");
        public final static Property Week = new Property(3, int.class, "week", false, "WEEK");
    }


    public LoggDao(DaoConfig config) {
        super(config);
    }
    
    public LoggDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOGG\" (" + //
                "\"DEVICE_ID\" TEXT," + // 0: deviceId
                "\"CREATE_TIME\" TEXT," + // 1: createTime
                "\"DATA\" TEXT," + // 2: data
                "\"WEEK\" INTEGER NOT NULL );"); // 3: week
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOGG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Logg entity) {
        stmt.clearBindings();
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(1, deviceId);
        }
 
        String createTime = entity.getCreateTime();
        if (createTime != null) {
            stmt.bindString(2, createTime);
        }
 
        String data = entity.getData();
        if (data != null) {
            stmt.bindString(3, data);
        }
        stmt.bindLong(4, entity.getWeek());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Logg entity) {
        stmt.clearBindings();
 
        String deviceId = entity.getDeviceId();
        if (deviceId != null) {
            stmt.bindString(1, deviceId);
        }
 
        String createTime = entity.getCreateTime();
        if (createTime != null) {
            stmt.bindString(2, createTime);
        }
 
        String data = entity.getData();
        if (data != null) {
            stmt.bindString(3, data);
        }
        stmt.bindLong(4, entity.getWeek());
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Logg readEntity(Cursor cursor, int offset) {
        Logg entity = new Logg( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // deviceId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // createTime
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // data
            cursor.getInt(offset + 3) // week
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Logg entity, int offset) {
        entity.setDeviceId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setCreateTime(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setWeek(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Logg entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Logg entity) {
        return null;
    }

    @Override
    public boolean hasKey(Logg entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
